@model IEnumerable<kaoshi.Models.es_stu_exam>

@{
   ViewBag.Title = "Record";
}

<h2>考试记录</h2>

<table class="table">
   <tr>
      <th>考试名称</th>
      <th>科目</th>
      <th>考试时间</th>
      <th>(答题/提交)时间</th>
      <th>得分</th>
      <th></th>
   </tr>

   @foreach (var item in Model)
   {
      <tr>
         <td>
            @Html.DisplayFor(modelItem => item.es_exam.es_paper.name)/
            (@Html.DisplayFor(modelItem => item.es_exam.es_paper.ab_paging))卷
         </td>
         <td>
            @Html.DisplayFor(modelItem => item.es_exam.es_paper.es_subject.name)
         </td>
         <td>
            @Html.Raw(string.Format("{0:g}", item.es_exam.begin_time))-@Html.Raw(string.Format("{0:t}", item.es_exam.end_time))
         </td>
         <td>
            @Html.Raw(string.Format("{0:g}", item.begin_time))-@Html.Raw(string.Format("{0:t}", item.end_time))
         </td>
         <td>
            @Html.DisplayFor(modelItem => item.score)
         </td>
         <td>
            @Html.ActionLink("详情", "Details", new { id = item.id })
         </td>
      </tr>
   }

</table>
